apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageUpdateAutomation
metadata:
  name: parser-telegram-update-automation
spec:
  imageRepositoryRef:
    name: parser-telegram
  update:
    strategy: Setters
    fieldRef:
      apiVersion: apps/v1
      kind: Deployment
      name: parser-telegram
      namespace: default
  git:
    repository: https://github.com/gt005/news_aggregator_infra.git
    branch: main
    secretRef:
      name: git-credentials
