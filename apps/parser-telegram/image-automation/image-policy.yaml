apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: parser-telegram
spec:
  imageRepositoryRef:
    name: parser-telegram
  filterTags:
    pattern: "^(?P<commit>[a-f0-9]{40})$"
    extract: "$commit"
  policy:
    numerical:
      order: asc
